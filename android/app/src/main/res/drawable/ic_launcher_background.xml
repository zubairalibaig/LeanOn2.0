<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon background. Mirrors the site's hero gradient so the launcher
     icon and the splash read as the same surface. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="#C2E4F2"
        android:centerColor="#EAF6FB"
        android:endColor="#FFFFFF" />
</shape>
